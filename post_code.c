// This code is to be appended to the final cisc file created by our compiler
PUSH(R0);
CALL(WRITE_SOB);
CALL(NEWLINE);

	lnot_proc:
  l_NOT_VALID_ARGUMENTS:	
  l_NOT_PAIR:
  STOP_MACHINE;

  return 0;
}
